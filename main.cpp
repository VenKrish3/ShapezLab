#include <iostream>
#include <cassert>
#include <iomanip>
#include "Shape.hpp"
#include "Circle.cpp"
#include "Rectangle.cpp"
#include "RightTriangle.cpp"

using namespace std;

int main()
{
	int x = 3;
	int y = 3;
	Circle circle(x);
	Rectangle rect(x,y);
//	assert(circle.getArea() == 28.27);
//	assert(rect.getArea() == 9);
//	assert(trang.getArea() == 4.5);
//	assert(circle.getPerimeter() == 18.84);
//	assert(rect.getPerimeter() == 12);
//	assert(trang.getPerimeter() == 4.24);
}
